# ============================================================================
# Policy (2026-06-01): keep ALL source scripts (.py/.sh) and docs
# (.md/.txt/.bib/.tex) in git so the two work machines stay consistent.
# IGNORE only: experiment results, raw datasets, scratch dirs, binaries, caches.
# temp/ and .trash/ are ALWAYS excluded.
# ============================================================================

# --- Python caches / build (not source) ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# --- Backups (not canonical scripts) ---
*.backup
*.backup_*
*_backup*.py
*_option*.py
*.bak

# --- Jupyter / IDE / OS ---
.ipynb_checkpoints/
*.ipynb
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Binaries / images / archives / logs / model weights / arrays (not scripts or docs) ---
*.png
*.jpg
*.jpeg
*.gif
*.svg
*.pdf
*.zip
*.gz
*.tar
*.tgz
*.7z
*.log
*.pt
*.pth
*.ckpt
*.npz
*.npy
*.bin
*.csv
*.parquet

# --- Experiment results / raw data / scratch / external refs / paper (NOT in-repo source) ---
results/
dataset/
temp/
.trash/
trash/
legacy/
references/
ResearchProposal/
comparison/results/
docs/report_images/
# paper/ (2026-06-10): 신규 논문 워크스페이스는 매 Phase 게이트마다 commit 대상 (MASTER_ORCHESTRATION_PROMPT §4).
# 이전 작업물은 paper_legacy/로 이동되어 아래에서 ignore. LaTeX build 부산물만 제외.
paper_legacy/
paper/07_latex/build/
paper/**/*.aux
paper/**/*.synctex.gz
.claude/
CLAUDE.md
.poe/
__marimo__/

# --- Junk / probe files ---
$LOG
.q*check*
.zzz_probe.txt
